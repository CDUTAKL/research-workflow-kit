experiment_id: EXP-101
seed: 42
split: smoke_chronological_60_20_20
metric: field_coverage
output: outputs/EXP-101-smoke
data:
  source_path: tests/fixtures/evcs_sessions_smoke.csv
  timestamp_field: connectionTime
  required_fields:
    - connectionTime
    - disconnectTime
    - doneChargingTime
    - kWhDelivered
    - stationID
  node_candidates:
    - spaceID
    - stationID
  blocked_feature_fields:
    - disconnectTime
    - doneChargingTime
    - kWhDelivered
  split_rule:
    train: 0.6
    validation: 0.2
    test: 0.2
