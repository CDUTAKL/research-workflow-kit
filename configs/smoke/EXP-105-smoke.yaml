experiment_id: EXP-105
seed: 42
split: smoke_chronological
metric: missed_risk_rate
output: outputs/EXP-105-smoke
data:
  predictions_path: outputs/EXP-104-smoke/predictions_quantiles.csv
  timestamp_field: timestamp
  node_field: node_id
  target_field: load
risk:
  threshold_source: smoke_train_quantile_proxy
  threshold_quantiles: [0.9, 0.95]
  methods:
    - point_threshold
    - interval_upper_bound
