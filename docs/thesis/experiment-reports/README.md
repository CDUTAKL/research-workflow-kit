# Experiment Reports

Use this folder for lightweight experiment review reports generated from registry rows, metrics files, autoresearch TSV records, and environment snapshots.

Reports should help decide whether an `EXP-*` run can support a `CLM-*` thesis claim.
